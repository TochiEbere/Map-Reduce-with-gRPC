syntax = "proto3";

package driver_package;

service Driver {
    rpc launch(Data) returns (Status);
}

message Data {
    string file_path = 1;
    string ports = 2;
    int32 m = 3;
}

message Status {
    string msg = 1;
    int32 code = 2;
}

message empty{}